# Audit report — 2026-08-07

Re-run of the recurring `skills/meta-audit/` audit. Method: full re-discovery of the real
tree (never memory), judged against the project's own governance as it exists today.

## Real state (re-discovered)

- Repo: `ui-agent-kit` (git, GitHub `TheophileBaudouin/ui-agent-kit`, main), npm package
  `ui-agent-kit` (0.1.0) published from the repo root.
- Meta root: `AGENTS.md`, `governance/`, `references/`, `skills/meta-audit/`, `cli/`
  (installer), `package.json`, `README.md`, `LICENSE`, `.pi/settings.json`,
  `.markdownlint-cli2.jsonc`, `.pi-lens.json`. SDK: `sdk/` (autonomous, own AGENTS.md +
  configs + skills). `ui-workspace/` gitignored (buildable mirror).
- Phases: 0–5 done + validated; Phase 7 (installer) done at Théo's request; Phase 6 pending.

## Findings

| Severity | Finding | Fix applied |
| --- | --- | --- |
| 🔴 | None. | — |
| 🟠 | Governance state files stale: `governance/phases.md` and root `AGENTS.md` had no trace of the installer (Phase 7) — contradicts "state files reflect reality" (constitution) and the session-0 gotcha. | Added Phase 7 row + history to `phases.md`; updated `AGENTS.md` state, "Where to find what" (`cli/`), useful commands (install + `npm run manifest`/`test`/`publish`); wrote `references/phase-7-report.md` (traces the 4 ask_user decisions — axis g). |
| 🟠 | French leftover in `sdk/skills/web-platform-guidelines/SKILL.md` frontmatter description (rename note) — language rule (Théo's decision). | Translated to English. |
| 🟠 | Two `cli/` files reformatted to tabs by an unsolicited auto-format hook → mixed style with the rest of `cli/` (2 spaces). | Reverted both files (uniform 2-space). |
| 🟡 | No `LICENSE` file while `package.json` declares MIT and README claims it (npm publish warns). | Added standard MIT `LICENSE`. |
| 🟡 | SDK skills not Pi-discoverable from this repo's root (`.pi/settings.json` declared only `../skills`) — axis d. | Added `../sdk/skills` to root `.pi/settings.json`. |
| 🟡 | README step 1 wording still described the old knowledge-only `ui-kit/` copy. | Updated to the whole-folder mirror wording. |

## Verified conform (no action needed)

- 🟢 Constitution versioned (1.1.0) and respected; phases 1–5 each have a report; roadmap
  now matches reality (post-fix).
- 🟢 Meta/SDK separation (principle 6): no SDK content at root; `sdk/` references nothing
  outside itself; `cli/`/`package.json`/`README` are meta tooling, not SDK content.
- 🟢 Authoring guides: 9 guides in `sdk/docs/authoring-guides/` covering the 6 `ux/` files +
  ui-rules + patterns + components-index, each answering purpose/format/read/update.
- 🟢 Sourcing: `ui-rules/` and `patterns/` cite verifiable sources (spot-checked); research
  log consistent with installed skills; first-party skills (`ux-memory`, `ui-review`) traced
  in phase reports 3–5.
- 🟢 Skills functional: all 7 SDK skills + `meta-audit` on disk; declared via
  `.pi/settings.json` (root, SDK, and consumer install).
- 🟢 Links: every relative markdown link resolves (script-checked).
- 🟢 Names: no `ui-dev-kit` leftover in versioned files; name `ui-agent-kit` consistent
  (README, package.json, governance).
- 🟢 Language: English everywhere except the one found and fixed.
- 🟢 Installer: 11 e2e tests green, manifest generated, import self-check active.

## Summary for Théo

Project healthy: no blocking point. Three 🟠 coherence fixes (stale governance state, one
French leftover, an unsolicited reformat) and three 🟡 polish items (LICENSE, root skills
declaration, README wording) — all fixed, verified, and included in this commit. Next
decision: whether to run Phase 6 (continuous audit) on a schedule, or keep audits manual.
