# Audit report — ui-dev-kit meta-project

> Date: August 6, 2026 · Auditor: independent review pass (no structural changes applied —
> all structural corrections are listed as recommendations below).

## 0. Cartography of what exists (step 0)

- **Project name (real)**: `ui-dev-kit`.
- **Location (real)**: a standalone folder at `~/Documents/devellopement/workspace` —
  **not a git repository** (no `.git`). The decision "separate repository" was made by Théo
  (session-0-report §4) but no `git init` was ever performed.
- **Structure (real)**: `AGENTS.md`, `README.md`, `governance/` (constitution, phases,
  parallel-work, 9 authoring guides), `skills/` (7 skills), `ux/` (6 empty templates),
  `ui-rules/` (10 filled), `patterns/` (8 filled), `references/` (12 reports + research log +
  design systems + wails constraints), `ui-sdk/` (40 real source pieces + index + docs),
  `ui-workspace/` (temporary buildable project: 61 shadcn components, node_modules,
  gitignored), plus project configs (`.gitignore`, `.markdownlint-cli2.jsonc`, `.pi-lens.json`).
- **Execution state (real)**: phases 1–5 were executed and each was explicitly validated by
  Théo (transcripts + Progress.md memory + 5 phase reports). The SDK exists: `ui-sdk/`
  contains 40 real `.tsx/.ts` pieces (harvested, license-verified) and `ui-workspace/`
  contains the frozen base (61 components, hash-verified intact).

## 4. Priority axis — meta-project / SDK separation

### 4.1 Premature SDK construction?

**Verdict: no premature construction — but see 4.2.**

- `ui-sdk/` contains real code (40 pieces). This is NOT a violation of the *current*
  governance: `governance/phases.md` plans phases 1–5 (SDK construction), and each phase
  report + Progress.md trace Théo's validation. The construction followed the validated
  roadmap.
- The frozen base (61 shadcn components) lives **only** in `ui-workspace/` (temporary,
  gitignored, explicitly planned in phases.md). **No frozen-base component is duplicated in
  `ui-sdk/`** (verified: 0 name collisions). No stray shadcn downloads elsewhere.
- ✅ Conforme (construction) — the plan was followed and validated.

### 4.2 Explicit written boundary

**Verdict: boundary exists but is stale — 🔴 blocking.**

- `AGENTS.md` still states: *"Current state: meta-project under construction (Phase 0).
  Nothing is built in `ui-sdk/` today"* and the "Where to find what" table still says
  `ui-rules/` and `patterns/` are *"empty templates → Phase 4"* and `ui-sdk/` is
  *"from Phase 1 only"*.
- `governance/phases.md` still states: *"Only Phase 0 (this session) is executed"* and lists
  *"Forbidden: any content in `ui-sdk/components|blocks|layouts|templates|examples`"*.
- A fresh agent reading the governance would believe nothing is built and that writing into
  `ui-sdk/` is forbidden — while the opposite is true (phases 1–5 done, SDK populated). This
  breaks the reliability of the instructions (cases 2+3 of the mission's deviation rules:
  undocumented + internal incoherence).
- The meta/SDK *physical* separation itself is clear and documented (governance vs
  `ui-sdk/`+`ui-workspace/`; `CONSUMPTION.md` explains the model) — only the **state
  description** is wrong.
- ➡️ **Recommendation (Théo to approve)**: update `AGENTS.md` §"This project" (state = SDK
  built through Phase 5, kit operational) and its table; update `phases.md` (mark phases
  1–5 done, replace "allowed now (Phase 0)" with the current-phase section, remove the
  obsolete "Phase 1 entry" block).

### 4.3 Purity of rule files

**Verdict: ✅ Conforme.** `ui-rules/` (10 files, ~40 lines each) and `patterns/` (8 files)
contain rules, explanations and short illustrative snippets (3–6 lines) only. No rule file
has drifted into a full component/page deliverable.

### 4.4 Skills govern, don't build

**Verdict: ✅ Conforme.** The 7 skills are guards (`ux-memory`, `ui-review`) and reference
skills (shadcn, frontend-design, web-design-guidelines, macos/web-platform-guidelines) —
guidance and checklists, not ready-to-paste SDK components. The shadcn skill ships CLI
examples, which is its documented upstream content.

### 4.5 Separation readable despite structure

**Verdict: ✅ Conforme.** Meta (governance/skills/ui-rules/patterns/references) vs SDK
(ui-sdk/ + temporary ui-workspace/) is unambiguous at the folder level and documented in
README + CONSUMPTION.md, even though there is no git repo.

## 5. Other audit axes

**a. Governance coherence — 🟠.** Constitution exists, versioned 1.0.0, principles respected
(frozen base hash-verified 61/61; naming rule honored; sources cited). But the two
state-description files (AGENTS.md, phases.md) are stale (see 4.2). AGENTS.md is 78 lines —
still short, ok. Phases roadmap not updated to reflect executed phases (the biggest gap).

**b. Authoring guides / memory systems — ✅.** 9 guides exist and every living memory system
has one: `ux/` (6 files → 6 guides), `ui-rules/` (1), `patterns/` (1), `components-index`
(1). Sample-verified (ux-user-flows.md): all four required questions answered, including the
analysis method for updates.

**c. Sourcing — 🟡.** Every rule in ui-rules/patterns cites a source (verified at Phase 4 +
cleanup). Research log (§A–§G) is coherent with the 5 reference skills installed. Gap: the
two in-house guards (`ux-memory`, `ui-review`) are not mentioned in the research log — their
existence is traced in session-0-report §2 but the log itself is incomplete as the
skills↔disk cross-reference. ➡️ Recommendation: add a line to research-log §E listing the
two in-house skills (or an explicit pointer to session-0-report).

**d. Skills actually functional — ✅.** 7/7 skill folders present in `skills/` with
`SKILL.md` containing `name` + `description` (verified). Discovery mechanism documented:
`.pi/settings.json` → `{"skills": ["../skills"]}` (root skills/ is not auto-discovered —
correction documented in research-log §A).

**e. Names and links — ✅ (with 1 fix applied).** Project name "ui-dev-kit" consistent across
AGENTS.md, README, constitution, phases, reports. Internal links were scanned in the cleanup
pass (1 broken link fixed: `patterns/dashboard.md` → evilcharts/charts path). Two French
fragments in `references/cleanup-report.md` (quotes) were translated during this audit
(explicit corrections).

**f. Language uniformity — 🟢.** All project files in English per the documented decision
(gotcha + Agent.md rule). Only "Théo" (proper noun) retains accents.

**g. Decision traceability — 🟡.** The three Théo decisions (name, separate location,
100% free blocks) are traced in session-0-report §4. Gap 1: "separate repository" was
decided but no git repository exists — the project is a plain folder with no version control
or backup. Gap 2: per-phase validations by Théo are only traced in the gitignored memory
(Progress.md) and reports that end with "awaiting Théo's validation" — the validation itself
is not recorded in a versioned file. ➡️ Recommendations: `git init` + first commit; add a
"Validated by Théo on &lt;date&gt;" line to each phase report.

## Per-folder walkthrough

| Folder | Verdict | Reason (one line) |
| --- | --- | --- |
| governance/ | 🟠 | constitution/parallel-work/guides solid; **phases.md stale** (4.2) |
| skills/ | ✅ | 7/7 valid SKILL.md, discovered via settings.json, guards + references |
| ux/ | ✅ | 6 empty templates + 6 guides; empty is expected (waits for real product decisions) |
| ui-rules/ | ✅ | 10 filled, sourced, pure (rules + snippets only) |
| patterns/ | ✅ | 8 filled, sourced, template-complete |
| references/ | ✅ | research log, design systems, wails constraints, 5 phase reports, cleanup report, session-0 report all present and consistent |
| ui-sdk/ | ✅ | 40 pieces, license-verified, indexed (40↔40), CONSUMPTION.md contract, no frozen-base duplication |
| ui-workspace/ | ✅ | temporary frozen-base project, gitignored, build green, hash-verified intact |
| AGENTS.md | 🔴 | state description stale (4.2) |
| README.md | ✅ | accurate human view |

## Findings by severity

### 🔴 Bloquant (1)

1. **Governance state files stale** — `AGENTS.md` and `governance/phases.md` describe the
   project as Phase 0 / "nothing built / ui-sdk forbidden" while phases 1–5 are executed and
   the SDK is populated. This breaks instruction reliability for any future agent.
   → Correction: update both files to reflect the real state (approved by Théo first).

### 🟠 À corriger (2)

1. **No git repository** despite the "separate repository" decision — no version control,
   no backup, no history. → `git init` + first commit (Théo to approve; note the current
   `.gitignore` already excludes the right things).
2. **Théo's per-phase validations not versioned** — only in gitignored memory. → add a
   validated-by line to each phase report.

### 🟡 Mineur (2)

1. Research log §E doesn't list the two in-house guards (ux-memory, ui-review) — traced in
   session-0-report but the log is the wrong cross-reference to point to. → add a line.
2. (Optionally) the obsolete "Phase 1 entry" section in phases.md — absorbed by fix 1.

### 🟢 Conforme (explicitly verified)

- Meta/SDK physical separation, frozen base not duplicated, ui-workspace temporary + gitignored.
- 7 skills valid + discoverable; name consistent; sources cited; language uniform; guides complete.
- Phase reports 1–5 present and consistent with Progress.md.

## Summary for Théo

The project is healthy and coherent: meta and SDK are cleanly separated, everything is
sourced and documented. **One blocking point**: the governance files (AGENTS.md, phases.md)
still describe the project as "Phase 0, nothing built" while phases 1–5 are done — a future
agent would get wrong instructions. The only question to decide first: **update these two
files to reflect the real state (and optionally init the git repo that was decided but never
created)**.
