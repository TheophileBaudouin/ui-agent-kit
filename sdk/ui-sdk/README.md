# ui-sdk — components, blocks, layouts

> Source library of the ui-agent-kit. Populated from license-verified registries. Nothing here replaces the
> frozen base: every piece builds on top of it.

## Content

| Folder | Content |
| --- | --- |
| components/ | 33 added components (charts, settings/auth/billing, upload, sections) — Ready |
| blocks/ | 7 page blocks (command menu, login, onboarding, stats…) — Ready |
| layouts/ | Desktop layout templates — **reserved** |
| templates/ | Complete pages — **reserved** |
| examples/ | Usage examples — **reserved** |
| docs/ | Consumption guide, authoring conventions |
| components-index.md | Index of every piece |

## How to use

Copy the pieces you need into your app. Read `docs/CONSUMPTION.md` first — the copy rules
(whole folder, keep path, install per-piece npm deps) are the contract.

## Licensing

All pieces are MIT or Apache-2.0 (per-piece provenance in each README and in
components-index.md). The frozen base (shadcn/ui) is MIT.

## Status

Status: operational — index up to date, README + preview per piece, rules populated.
